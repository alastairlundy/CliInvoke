name: package-installation-choice-eval
description: Evaluation suite for the package-installation-choice skill
skill: package-installation-choice
version: "1.0"
config:
  trials_per_task: 1
  timeout_seconds: 180
  parallel: false
  executor: copilot-sdk
  model: gpt-5-mini
metrics:
  - name: accuracy
    weight: 1.0
    threshold: 0.8
tasks:
  - "tasks/*.yaml"
